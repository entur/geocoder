common:
  env: prd
  replicas: 4
  container:
    cpu: 0.25
    memory: 640
  ingress:
    host: geocoder-proxy.entur.io
    trafficType: api
  hpa:
    spec:
      metrics:
        - type: Resource
          resource:
            name: cpu
            target:
              type: Utilization
              averageUtilization: 80
      maxReplicas: 10
      minReplicas: 4
