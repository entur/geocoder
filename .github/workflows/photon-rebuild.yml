name: Photon — Rebuild & Deploy

on:
  workflow_dispatch:
    inputs:
      target:
        description: 'Deploy target'
        required: true
        type: choice
        options:
          - 'dev only'
          - 'dev → tst → prd'
          - 'tst → prd'
      nominatim_data_tag:
        description: 'Nominatim data tag'
        required: false
        type: string

concurrency:
  group: "photon-${{ inputs.target }}"
  cancel-in-progress: true

jobs:
  log-inputs:
    name: Log Inputs
    runs-on: ubuntu-latest
    steps:
      - name: Log manual inputs
        run: |
          {
            echo "### Manual Workflow Inputs"
            echo "**Target:** \`${{ inputs.target }}\`"
            echo "**Nominatim data tag:** \`${{ inputs.nominatim_data_tag }}\`"
          } >> "$GITHUB_STEP_SUMMARY"

  # Fresh tag for the rebuilt photon image and its photon_data upload. The input
  # nominatim_data_tag stays as-is - it points at an artifact published in a previous
  # workflow run, so reusing it here would collide with the existing docker image.
  generate-tag:
    name: Generate Tag
    uses: ./.github/workflows/_generate-tag.yml

  photon:
    name: Photon Image
    needs: generate-tag
    runs-on:
      group: grp-ubuntu-24.04-8core-x64
    permissions:
      actions: read
      contents: read
      id-token: write
      security-events: write
      pull-requests: write
    outputs:
      image_tag: ${{ steps.build.outputs.image_tag }}
    steps:
      - uses: actions/checkout@v6
      - name: Build Photon image
        id: build
        uses: ./.github/actions/build-photon-image
        with:
          nominatim_data_tag: ${{ inputs.nominatim_data_tag }}
          build_tag: ${{ needs.generate-tag.outputs.tag }}
          workload_identity_provider: ${{ vars.CI_WORKLOAD_IDENTITY_PROVIDER }}
          service_account: ${{ vars.CI_SERVICE_ACCOUNT }}

  deploy:
    name: Deploy
    needs: photon
    uses: ./.github/workflows/_photon-deploy-fanout.yml
    with:
      image: "geocoder-photon:${{ needs.photon.outputs.image_tag }}"
      target: ${{ inputs.target }}
    secrets: inherit

  notify-slack:
    needs: [ generate-tag, photon, deploy ]
    if: failure()
    runs-on: ubuntu-latest
    steps:
      - uses: slackapi/slack-github-action@v3
        with:
          webhook: ${{ secrets.SLACK_WEBHOOK_URL_HENRIK }}
          webhook-type: incoming-webhook
          payload: |
            text: "❌ *${{ github.workflow }}* (${{ inputs.target }}) failed: <${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}|run>"
